{"status":"success","content":"most people are paying $2,000 to 5,000 per month to SEO agencies to rank on Google and honestly I get it seo feels impossible you're told you need to write dozens of blog posts do keyword research for hours build internal links create pillar content it's overwhelming but here's what those expensive agencies don't want you to know i just built an entire SEO system that does all of this automatically for less than a dollar per week while everyone else is manually writing blog post and hoping Google notices them I'm using AI agents and NAN workflows to post two perfectly optimized SEO blogs every single week without me lifting a finger the system finds trending keywords writes the content adds internal and external links creates images and publishes everything to WordPress completely automated and the crazy part this isn't some theoretical concept i'm literally using this exact system right now to rank for competitive keywords in the AI space and I'm about to show you the entire workflow every node every prompt every automation so you can copy it and start ranking for your own keywords because here's the thing while your competitors are still paying thousands to agencies or spending 20 hours a week writing blogs manually you're going to have a system that works 247 in the background consistently pushing your website higher in Google's rankings so if you ever wanted to dominate SEO without the massive time investment or the expensive monthly bills this is exactly what you need to see i added the AI tools that we're going to use in the description so you can check them out let me show you how it works before we start building it step by step we'll start by executing the workflow after we execute the workflow the whole process runs automatically from start to finish moving through five key stages planning making HTML and inline links creating titles slugs and summaries image generation with Nano Banana and posting it begins in Google Sheets flows through planning and content creation generates HTML adds optimized metadata produces hyperrealistic images and ends with a finished blog published directly on WordPress all right the run kicks off with planning where four AI agents preliminary plan research final plan and writing work together to structure and refine the content the Google sheet nodes only pull in topics labeled as not completed which ensures every new post is based on fresh ideas looking at the sheet you'll see the imported columns cluster intent keywords primary keyword and completed status all guiding how each blog is built next comes making HTML and inline links at this stage the workflow gathers completed rows from the sheet builds internal links into the structure and generates HTML that's ready for direct publishing into WordPress after that is creating titles slugs and summaries where the system automatically generates each piece based on the content created earlier these elements play a critical role in both SEO and readability giving every post a polished and search friendly presentation the fourth stage is image generation with Nano Banana using the title and summary as input Nano Banana creates hyperrealistic images tailored to the post known as a powerful image generation tool it ensures each article has professional visuals without requiring extra design work finally the workflow moves into posting the completed blog is published in WordPress and the Google sheet updates automatically to mark the post as completed from the completed sheet you can click the generated URL to open the live blog right away this workflow automates everything because doing all of that manually research writing formatting illustrations and publishing can take hours for just one post and often requires a full team here the workflow takes care of every post stay consistent back research illustrated with realistic visuals and tracked in sheets so progress is always clear it reduces repetitive work speeds up publishing and delivers a professional quality blogs with minimal effort you've already seen how the full workflow is running from start to finish so now let's break it down piece by piece the first step will be making sure the workflow can start on command or on a schedule and that it's always pulling in fresh topics from Google Sheets this is where the triggers and the first sheet connection come into play we will start with two triggers a manual trigger and a schedule trigger the manual trigger will let us run the workflow in a time on demand which is perfect for testing or urgent publishing the schedule trigger will handle everything automatically at set intervals generating and publishing new posts to WordPress without us needing to step in for this demo we'll leave the schedule at its default but you can adjust it to fit different use cases having both triggers will give us flexibility we can run the workflow manually when we want a quick control or rely on a schedule to keep content flowing consistently once that's set the next step will be connecting the system to Google Sheets so it always works with fresh topics to do this we'll add a Google Sheets get row and sheet node we'll set resource to sheet within document and operation to get rows then we'll select the document from the list choose the target sheet and add a filter so the workflow only looks at rows where the completed column is set to no that way we're only pulling in topics that haven't been published yet there's one more option we'll turn off return only the first matching row instead of pulling every row marked no this makes the node return just the first available one let's execute the step here the node pulls the seventh row from the sheet which is the first row with a completed column is set to no this flow ensures the workflow always starts with a valid trigger and fresh content keeping the process clean and consistent now it's time to turn raw topics into something usable this preliminary plan agent will spin those ideas into a rough outline we can actually build on instead of starting from scratch this step is giving a structure direction and a draft we can refine later in order to set it up we're going to add an AI agent node into the workflow or the configuration we'll choose source for prompt as defined below then we'll paste in the prepared script for the user message and the system message once that's ready we'll rename the node to keep it clear so it's obvious what the stage is doing next we'll add a chat model and select GPD5 for reasoning in order to push the outputs even further we'll also attach a think tool giving the agent more depth and accuracy in its responses after executing the step the agent will generate a preliminary plan a rough but usable draft that guides the blog writing and posting process the preliminary plan agent makes sure the content creation process begins with a clean framework without locking into final details too early it captures broad ideas possible angles and a rough flow giving the workflow material to build on now think of it as sketching the blueprint it speeds up the creative process reduces blank page hesitation and sets up the final plan agent to refine and polish the draft into a well ststructured readytouse plan however a plan without research is just guesswork so the next step is giving your outline some real banking the research agent is handling that pulling in references evidence and credible sources that make the content trustworthy we don't want to just leave the draft floating on ideas alone this stage is grounding it in facts and data we can actually site later setting up is straightforward we are going to add an AI agent now let's start configuring it i'm going to set the source for prompt as defined below the prepared script will go into both the user message and the system message here you can see the prompts clearly and these are the instructions that will drive the agent after that the node gets renamed to research agent so its role in the workflow stays clear for the model we're choosing Perplexity Sonar Pro since it specializes in pulling information from valid sources that way the workflow isn't just generating files it will gather citations facts and references the later agents can actually use running this step outputs a collection of relevant sources tied to the topic at hand now that we have this here the workflow has a stronger knowledge base reducing the risk of misinformation and setting up the final plan and writing agents with richer material to refine into a finished post the brainstorming and research give us raw material but this is where everything starts taking shape the final plan agent will be pulling the preliminary draft and research together into a structured outline that's ready for execution it will organize the topic search intent and keywords into a single plan that will guide the writing agent in the next stage setting it up means adding another AI agent node into the workflow and configuring it just like before source for prompt as defined below we'll paste in again the prepared script for both the user message and the system message once that's done we'll rename the node to final plan agent so its role is easy to identify here's where this step gets more specific if any red lines appear from mapping mismatches we'll resolve them by linking the fields from the Google sheet now mapping keywords from the sheets keywords field search intent from the intent field and primary keyword from the primary keyword field that way the agent is working directly with the right data for the model we'll select GPD5 to handle reasoning and we'll add a think tool to push its outputs further making sure the outline is sharp and comprehensive let's execute this one there it is the output is no longer just a draft as it is now a clear actionable plan the final plan agent removes any ambiguity ensures SEO alignment and produces a roadmap that makes the writing stage smooth and consistent let's turn plans into pros we're going to take everything the workflow has prepared the preliminary plan the research agent sources and the final plan and hand it to a writing agent that will generate the full article end to end no more outlines here we're going to produce a complete detailed draft that's ready for formatting and publishing we'll add an AI agent node and set source for prompt to defined below then as usual paste in the prepared script for both the user message and the system message after that we'll follow the same process we're renaming the node writing agent so its role stays unmistakable inside the canvas of nodes for the model I'll select claw set form to handle the writing itself clear structure natural flow and depth in long- form output to strengthen reasoning and coherence we're also going to attach a think tool and in order to keep numbers formulas and quick checks reliable we're adding a calculator tool as well let's put this in action after executing this step the agent will pull from the final plan and weave in the research agent citation and the preliminary plan's direction which turns structured inputs into readable publication ready content keep in mind that if we skip this step the workflow would stall planning this agent handles the actual writing taking care of the heavy lifting automatically every blog post becomes more powerful when it connects back to what's already been published we don't want users to manually dig through old posts and add links one by one so we're letting the workflow pull them in automatically this step is all about fetching completed blogs from Google Sheets and then combining them into a single data set that can be used for inline linking we'll start by dropping in a Google Sheets get row in sheet node in the configuration we'll set resource to sheet within document and operation to get rows from there we'll select the right document and the target sheet running the step will fetch multiple rows representing completed blog posts giving us the raw material for linking next we'll add an aggregate node for the configuration we'll set aggregate to all item data into a single list choose data for put input in field and include all fields now if we execute this step this will pull all the completed blog rows together and merge them into one clean block of structured data now the workflow has a single data set holding all past post ready to be referenced in the new article inline linking will happen seamlessly boosting SEO keeping readers on the site longer and making each blog feel more connected without adding any extra manual effort like what I mentioned earlier manually adding links into every blog post is tedious and easy to get wrong miss one and you lose a chance to boost SEO or keep readers on the site longer so after aggregating past posts into a single data set the next step is to put that data to work the inline links agent taps into it and automatically weaves relevant references into the draft we'll add an AI agent node into the workflow and set source for prompt to define below like before we'll paste in the prepared script for both the user message and the system message once that's ready we'll rename the node to inline links agent so its role is obvious inside the workflow here we'll select GPD5 again for the model to handle reasoning and we'll attach a thing tool to strengthen the outputs with more depth executing the step lets the agent scan the aggregated data set and generate inline references that slot seamlessly into the new blog post inline links aren't just nice extras they're essential they tell search engines how your posts relate to each other and help readers stick around to explore more the draft now comes preloaded with links placed exactly where they make sense stretching connections between posts guiding readers naturally and making every article richer and more professional without any manual effort now every blog post needs more than just text it also needs clean HTML that's ready to publish rather than formatting everything by hand we're going to let the workflow generate the code directly so the article can move straight into WordPress without delays in order to handle this we're going to use an open AI message a model node which is faster and more reliable for large structured outputs like HTML we'll add the node into the workflow and set resource to text and operation to message your model for the model we'll select GBD5 chat latest then we'll paste in the prepared script for both the user and system prompts and toggle on simplify output to keep the results clean here you can see the prompts that serve as the instructions to guide the output after executing the step we got here a block of clean HTML code ready to use and already structured for publishing this saves us from manual formatting reduces the risk of errors and keeps the workflow moving smoothly at scale before moving any further it's a good idea to get everything organized the workflow is already generating plans research HTML and a whole draft but if we don't structure those outputs things can get messy fast that's what the set field node is for it acts like a checkpoint pulling together all the important values and giving them clean labels so the rest of the workflow can reference them without confusion we'll add a set field node into the workflow and switch the mode to manual mapping from here we'll map each field to its source cluster from the sheet nodes cluster field intent from the intent field keyword from the keyword field html from the message a model nodes content final output from the writing agent final plan from the final plan agent research output from the research agent and preliminary plan from the preliminary plan agent after executing the step the workflow now has a clean standardized data set every key piece of information plans research keywords and generated content is neatly organized and ready to be used by the next nodes this makes the workflow more reliable reduces mapping errors and keeps it easy to manage even as it scales even with a full draft research and HTML in place the blog isn't ready to publish just yet what's missing are the final touches that make it professional and SEO ready a compelling title a clean URL slug and a concise summary the slug plus title plus summary agent will handle all of that automatically making sure every post has the metadata it needs before going live on WordPress to set this up we'll add an AI agent node it's the workflow and configure it with source for prompt defined below we'll paste in the prepared script for both the user message and the system message then toggle on require specific output format so the results stay structured for the model we'll select GPD5 again for reasoning also to sharpen the results we'll attach a thing tool and to make sure the metadata is formatted consistently we'll add a structured output parser finally we'll rename the node slug plus title plus summary agent so its purpose is clear let's execute the step the output now includes a finished slug title and summary that slot neatly into the publishing pipeline the title draws readers in the slug creates clean SEOfriendly URLs and the summary boosts readability while powering previews and social shares automating this step keeps everything consistent saves time and prevents errors this also guarantees that every post has the supporting fields WordPress needs transforming raw AI content into something fully production ready no blog feels complete without visuals images catch the eye break up long sections of text and make post feel more polished and professional we don't need to search for stock photos or design graphics manually we're going to let the workflow generate custom hyper realistic visuals using Nano Banana through Kie AI each image will be tied directly to the bug's title and summary so it's always on topic and relevant to build this we'll add an HTTP request note into the workflow from there we'll go to the nano banana model page on KI AI and paste the example C URL request provided back in N8 that request will be imported directly into the HTTP request node the next step is generating an API key from KIE AI's API key page and pasting it beside bearer in the authorization header after the setup is ready we'll replace the default JSON with map prompts to control the generation the title and summary are both pulled from the slug plus title plus summary agent node giving Nano Banana the exact context it needs to create relevant images executing the step shows the workflow producing a hyperrealistic image automatically through the HTTP request at this point the block isn't just written and structured it now comes with unique tailored visuals that elevate engagement and make every post look professional from the start but image generation doesn't always happen instantly depending on the complexity of the request Nano Banana may take a little time to process to prevent the workflow from moving too fast and trying to use an image before it's ready we're dropping in a weight node as a simple buffer adding the node is straightforward we'll configure it with a weight amount of one and set the weight unit to minutes this short pause gives the system enough breathing room to finish generating the image before the workflow continues after saving and executing the workflow pauses as expected holding for 1 minute before moving on to the next step that build and delay keeps everything smooth avoiding errors and ensuring the image is ready when the workflow needs it after giving Nano Banana enough time to finish the next step is actually retrieving the generated image the post request kicked off the job but it didn't return the final file right away to pull the completed image into our workflow we'll add a get HTTP request node inside and init we'll drop in the node and configure it using the C URL example provided on the nano banana model page that request is then pasted into the node so the workflow knows how to check the status of the image generation under task ID we'll map the dynamic value from the previous post node to make sure the request is tied to the right job for authorization we'll copy the API key we used earlier and paste it into the heather in bearer format after executing this step the workflow fetches the finished nano banana image right now the image is no longer pending and it's ready to be used in the blog post and carried forward into the publishing stage sometimes even after running the get request the image still isn't ready i don't want to let the workflow fail or try to move forward with an incomplete file here we can build in a simple loop that keeps checking until the image is fully processed this will be done using an if node paired with a weight node we'll start by adding an if node into the workflow for value one we'll map the state from the get request node the type is set to string and the condition is is equal to for value two we'll enter success this creates the condition if the state is not success the workflow will follow the false branch next we'll attach a weight node to the false branch the weight amount is set to 30 and the unit is set to seconds giving Nano Banana more time to finish process then we'll connect the wait node back to the get request node so the workflow loops until the state changes to success once this logic is in place the workflow keeps retrying automatically until the image is marked complete only then does it continue guaranteeing that the final image is always ready before moving into the publishing stage apis rarely return outputs in a perfectly clean format the image URL retrieve comes wrapped in JSON with braces brackets and extra characters that won't work well when passed directly into publishing nodes to fix that we're adding another set field node that cleans up the result JSON value and turns it into a simple usable string after dropping the node into the workflow we'll configure it by mapping the result JSON from the previous node the type is set to string and we'll use a short piece of prepared JavaScript to strip out the unwanted characters once everything is set we'll run this step we got here a clean URL free of the clutter that comes with raw JSON instead of risking mapping errors or broken links the workflow has a polished string that can be passed forward into the publishing stage without problems all right publishing usually means logging to WordPress formatting everything by hand and double-checking details here the workflow eliminates all of that hosting the final blog securely and consistently through automation using OOTH2 authentication the HTTP request node handles the publishing step directly creating a smooth end- to-end pipeline from planning to live article to build this we'll add an HTTP request node into the workflow and set the method to post the URL comes from the prepared script which changes depending on your WordPress site for authentication we'll select the generic credential type and use OOTH 2 API inside N we'll generate new credentials by creating an application in the WordPress developer resources then copy over the authorization URL access token URL client ID and client secret the scope will be set to global authentication goes in the body and once saved the connection is ready next we'll configure the request parameters heathers are enabled with content type set to application/json the body includes the title and slug from the slug plus title post summary agent node the content pulled in with a prepared JavaScript expression and the status set to publish let's execute this step the workflow now sends the complete blog post directly to WordPress the node returns a link that we can open to confirm the post is live at this point the entire process planning writing image duration formatting and publishing is fully automated a little reminder for those using WordPress.org or inside of WordPress.com the built-in WordPress node in N8N can replace the HTTP request method you simply paste in your WordPress credentials and map the fields and the workflow will publish just as smoothly the last piece of the pipeline is keeping records straight after a post goes live we don't want to track it manually in spreadsheets or risk forgetting to mark it as completed the workflow will handle this automatically with two simple steps updating the original sheet to mark the topic as finished and appending the finalized blog details into a dedicated completed sheet first we'll add a Google Sheets update row in sheet mode the resource is set to sheet within document and the operation to update row we'll select the same document and sheet used earlier then switch mapping column mode to manual per column to match on we'll choose row underscore number and map it from the Google Sheets node at the start of the workflow to make sure the right row is updated from there we are going to remove any unnecessary data and set the completed column to yes running the step confirms the update by returning a yes which means the block status is now marked complete next we'll add a Google Sheets append row in sheet node again resources set to sheet within document and the operation to append realm we'll select the document and then target the completed sheet in manual mapping mode we'll match each field carefully blog title from the slug plus title plus summary agent keywords from the get row sheet note summary from the slug plus title plus summary agent and the URL from the post notes link field executing this step successfully logs the entry into the completed sheet right now the loop is closed the workflow not only plans writes and publishes blog posts but also keeps a clean up-to-date record of every article that goes live from ideiation to publishing to recordeping the system is fully automated leaving no gaps to manage formally from ideiation to publishing to recordeping the system is fully automated leaving no gaps to manage manually thanks for watching i hope this breakdown helps you see how simple it is to run SEO on autopilot don't forget to check the tools in the description and I'll see you in the next one ","title":"I Built an Entire SEO Automation to Rank #1 on Google for $1 (AI Agent + N8N)","source_type":"youtube","url":null,"char_count":26295}
