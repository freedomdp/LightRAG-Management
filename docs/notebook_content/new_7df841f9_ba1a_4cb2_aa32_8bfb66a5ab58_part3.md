{
  "title": "I Built an Entire SEO Automation to Rank #1 on Google for $1 (AI Agent + N8N) (Часть 3)",
  "content": "e used by the next nodes this makes the workflow more reliable reduces mapping errors and keeps it easy to manage even as it scales even with a full draft research and HTML in place the blog isn't ready to publish just yet what's missing are the final touches that make it professional and SEO ready a compelling title a clean URL slug and a concise summary the slug plus title plus summary agent will handle all of that automatically making sure every post has the metadata it needs before going live on WordPress to set this up we'll add an AI agent node it's the workflow and configure it with source for prompt defined below we'll paste in the prepared script for both the user message and the system message then toggle on require specific output format so the results stay structured for the model we'll select GPD5 again for reasoning also to sharpen the results we'll attach a thing tool and to make sure the metadata is formatted consistently we'll add a structured output parser finally we'll rename the node slug plus title plus summary agent so its purpose is clear let's execute the step the output now includes a finished slug title and summary that slot neatly into the publishing pipeline the title draws readers in the slug creates clean SEOfriendly URLs and the summary boosts readability while powering previews and social shares automating this step keeps everything consistent saves time and prevents errors this also guarantees that every post has the supporting fields WordPress needs transforming raw AI content into something fully production ready no blog feels complete without visuals images catch the eye break up long sections of text and make post feel more polished and professional we don't need to search for stock photos or design graphics manually we're going to let the workflow generate custom hyper realistic visuals using Nano Banana through Kie AI each image will be tied directly to the bug's title and summary so it's always on topic and relevant to build this we'll add an HTTP request note into the workflow from there we'll go to the nano banana model page on KI AI and paste the example C URL request provided back in N8 that request will be imported directly into the HTTP request node the next step is generating an API key from KIE AI's API key page and pasting it beside bearer in the authorization header after the setup is ready we'll replace the default JSON with map prompts to control the generation the title and summary are both pulled from the slug plus title plus summary agent node giving Nano Banana the exact context it needs to create relevant images executing the step shows the workflow producing a hyperrealistic image automatically through the HTTP request at this point the block isn't just written and structured it now comes with unique tailored visuals that elevate engagement and make every post look professional from the start but image generation doesn't always happen instantly depending on the complexity of the request Nano Banana may take a little time to process to prevent the workflow from moving too fast and trying to use an image before it's ready we're dropping in a weight node as a simple buffer adding the node is straightforward we'll configure it with a weight amount of one and set the weight unit to minutes this short pause gives the system enough breathing room to finish generating the image before the workflow continues after saving and executing the workflow pauses as expected holding for 1 minute before moving on to the next step that build and delay keeps everything smooth avoiding errors and ensuring the image is ready when the workflow needs it after giving Nano Banana enough time to finish the next step is actually retrieving the generated image the post request kicked off the job but it didn't return the final file right away to pull the completed image into our workflow we'll add a get HTTP request node inside and init we'll drop in the node and configure it using the C URL example provided on the nano banana model page that request is then pasted into the node so the workflow knows how to check the status of the image generation under task ID we'll map the dynamic value from the previous post node to make sure the request is tied to the right job for authorization we'll copy the API key we used earlier and paste it into the heather in bearer format after executing this step the workflow fetches the finished nano banana image right now the image is no longer pending and it's ready to be used in the blog post and carried forward into the publishing stage sometimes even after running the get request the image still isn't ready i don't want to let the workflow fail or try to move forward with an incomplete file here we can build in a simple loop that keeps checking until the image is fully processed this will be done using an if node paired with a weight node we'll start by adding an if node into the workflow for value one we'll map the state from the get request node the type is set to string and the condition is is equal to for value two we'll enter success this creates the condition if the state is not success the workflow will follow the false branch next we'll attach a weight node to the false branch the weight amount is set to 30 and the unit is set to seconds giving Nano Banana more time to finish process then we'll connect the wait node back to the get request node so the workflow loops until the state changes to success once this logic is in place the workflow keeps retrying automatically until the image is marked complete only then does it continue guaranteeing that the final image is always ready before moving into the publishing stage apis rarely return outputs in a perfectly clean format the image URL retrieve comes wrapped in JSON with braces brackets and extra characters that won't work well when passed directly into publishing nodes to fix that we're adding another set field node that cleans up the result JSON value and turns it into a simple usable string after dropping the node into the workflow we'll configure it by mapping the result JSON from the previous node the type is set to string and we'll use a short piece of prepared JavaScript to strip out the unwanted characters once everything is set we'll run this step we got here a clean URL free of the clutter that comes with raw JSON instead of risking mapping errors or broken links the workflow has a polished string that can be passed forward into the publishing stage without problems all right publishing usually means logging to WordPress formatting everything by hand and double-checking details here the workflow eliminates all of that hosting the final blog securely and consistently through automation using OOTH2 authentication the HTTP request node handles the publishing step directly creating a smooth end- to-end pipeline from planning to live article to build this we'll add an HTTP request node into the workflow and set the method to post the URL comes from the prepared script which changes depending on your WordPress site for authentication we'll select the generic credential type and use OOTH 2 API inside N we'll generate new credentials by creating an application in the WordPress developer resources then copy over the authorization URL access token URL client ID and client secret the scope will be set to global authentication goes in the body and once saved the connection is ready next we'll configure the request parameters heathers are enabled with content type set to application/json the body includes the title and slug from the slug plus title post summary agent node the content pulled in with a prepared JavaScript expression and the status set to publish let's execute this step the workflow now sends the complete blog post directly to WordPress the node returns a link that we can open to confirm the post is live at this point the entire process planning writing image duration formatting and publishing is fully automated a lit",
  "source_type": "youtube"
}