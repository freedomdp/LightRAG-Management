{
  "title": "I Built an Entire SEO Automation to Rank #1 on Google for $1 (AI Agent + N8N) (Часть 4)",
  "content": "tle reminder for those using WordPress.org or inside of WordPress.com the built-in WordPress node in N8N can replace the HTTP request method you simply paste in your WordPress credentials and map the fields and the workflow will publish just as smoothly the last piece of the pipeline is keeping records straight after a post goes live we don't want to track it manually in spreadsheets or risk forgetting to mark it as completed the workflow will handle this automatically with two simple steps updating the original sheet to mark the topic as finished and appending the finalized blog details into a dedicated completed sheet first we'll add a Google Sheets update row in sheet mode the resource is set to sheet within document and the operation to update row we'll select the same document and sheet used earlier then switch mapping column mode to manual per column to match on we'll choose row underscore number and map it from the Google Sheets node at the start of the workflow to make sure the right row is updated from there we are going to remove any unnecessary data and set the completed column to yes running the step confirms the update by returning a yes which means the block status is now marked complete next we'll add a Google Sheets append row in sheet node again resources set to sheet within document and the operation to append realm we'll select the document and then target the completed sheet in manual mapping mode we'll match each field carefully blog title from the slug plus title plus summary agent keywords from the get row sheet note summary from the slug plus title plus summary agent and the URL from the post notes link field executing this step successfully logs the entry into the completed sheet right now the loop is closed the workflow not only plans writes and publishes blog posts but also keeps a clean up-to-date record of every article that goes live from ideiation to publishing to recordeping the system is fully automated leaving no gaps to manage formally from ideiation to publishing to recordeping the system is fully automated leaving no gaps to manage manually thanks for watching i hope this breakdown helps you see how simple it is to run SEO on autopilot don't forget to check the tools in the description and I'll see you in the next one ",
  "source_type": "youtube"
}