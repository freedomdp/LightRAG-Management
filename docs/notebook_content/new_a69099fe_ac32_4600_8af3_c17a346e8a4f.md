{"status":"success","content":"hi Michelle here today I'll show you how to scrape any website with web scraper appify offers over 2,000 web scraping and automation tools for e-commerce portals social media platforms travel websites and many other categories these work by filling out a simple input schema which is as specific for every tool or actor as we call them if you however need more specific data or you can't find an actor for the website you want to scrape you can use our generic web scraper this tool operates by loading web pages in a browser rendering Dynamic content and then using JavaScript code to fetch relevant data from the pages but first how does the scraper work it starts by fetching the start URL you set in the input loading it into the Chromium browser executing the page function and proceeding to find links if the link selector is filled out this process is then repeated until the scriper runs out of s URLs so let's take it further on head over to the link in the description and hit try for free you'll be taken to the signup page where you'll be asked to create an aify account don't worry it's free and no credit card is required now you land on the scraper setup page you can set the input via our userfriendly UI or Json let's go with the manual view first we'll need to choose the scraper run mode with development mode you'll be able to see what goes on under the hood during the scrape via Chrome's Dev tools this option is great if you want to debug and fine-tune your input for the initial run but since we're quite confident about we'll set up the actor let's set it straight to production next we'll choose the URL we want to script let's say we want data from the homepage of our open source web scraping Library Crawley if you want to CU links that the scripper stumbles upon you also have to set up the link selector and glob patterns however we're only getting text from a single page so we'll empty the link selector and skip this section next is a page function this is where you tell the scriptor which data points it should focus on it requires a bit of technological knowledge but we'll manage for our demo we want to extract the installation code from Cary or open source web scraping Library so let's select the code snippet hit inspect and find the element that covers the snippet you'll see it's part of the code block content low-b glass Let's test it out by going to the console and writing up a CSS selector that will grab this element and reveal the text inside remember to put a dot in the beginning to specify it's a CL there we go we got the code snippet so let's head back to our scrapers input in the page function we can delete the part that we don't need so everything between the context. jQuery and the return section is where we need to tell the scraper what we wanted to grab let's delete everything other than the URL since we want it in our output but this time we're not going to use the text content property but a jQuery call whose syntax goes like this we're going to name a property page code block tell the scraper we're going to be selecting choose a CSS selector and that we want to find all code blocks in the page and retrieve their text content however when we go back to the page and search this element we'll find there's multiple search code blogs on the page so let's add first open parenthesis close parenthesis before the text selector to only grab the first go block on the page and there you go our scraper setup but now that I think about it we actually have a page for cley for python which has the same structure as a cwy homepage in our input so let's add that one into the start URL and see if web scraper can handle both of our requests before running your actor would recommend saving your configuration and creating a task this can come in handy if you want to schedule or integrate your actor with other tools in the future or if you plan to work with multiple configurations with everything set up just hit start start to launch the actor you can watch your results appear in the overview or you can switch to the log tab if you want to see more details about the Run let's check our results there we go we have the installation codes of both Crawley and Crawley for python extracted in a structure format you can export these results in various formats such as Json CSV or as an htmma table and decide which fields to include or exclude then you can download your data set file by the way you can also run the script locally via API you can find the code in no JavaScript python or C URL in the API tab of the actor store Page to learn more about retrieving data programmatically check out our video on the subject if you want to automate your workflow even further you can schedule your actor to run regular intervals to do this choose your task and heit schedule you can then specify the frequency of how often you want to run the actor you can even connect your scraper to other cloud services such as Google Drive zap or any other appify actor and that's it after you set a web scraper for a website with ey specific structure you can scale your web scraping workflow by also crawling through links sister Pages or search results of any platform and remember if you encounter any complications please don't hesitate to report them to the actor developer through the issu app in the appify console if you like this video please leave a thumbs up subscribe and hit the Bell to stay notified about future content so long and thanks for all the likes ","title":"How to scrape ANY website with Apify’s Web Scraper API","source_type":"youtube","url":null,"char_count":5526}
